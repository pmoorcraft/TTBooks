!!! XML
%urlset{:xmlns => "http://www.sitemaps.org/schemas/sitemap/0.9"}
  - for blog in @blogs
    %url
      %loc #{blog_url(blog)}/
      %lastmod=blog.updated_at.strftime('%Y-%m-%d')
      %changefreq monthly
      %priority 0.5